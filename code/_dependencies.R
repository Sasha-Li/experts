if (!require("pacman")) install.packages("pacman"); library(pacman)

# load packages
p_load(
  tidyverse,
  assertr,
  brms,
  cowplot,
  glue,
  here,
  knitr,
  magrittr,
  R.rsp,
  readxl,
  rebus,
  tidybayes,
  viridis,
  scales,
  skimr,
  BayesFactor,
  devtools
)
